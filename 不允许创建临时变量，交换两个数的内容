//加法实现：
#include <stdio.h>  
int main()
{
	int a = 30;
	int b = 20;
	a = a + b;
	b = a - b;
	a = a - b;
	printf("a=%d,b=%d\n", a, b);
	return 0;
}
//乘法实现：
#include <stdio.h>  
int main()
{
	int a = 30;
	int b = 20;
	a = a * b;
	b = a / b;
	a = a / b;
	printf("a=%d,b=%d\n", a, b);
	return 0;
}
